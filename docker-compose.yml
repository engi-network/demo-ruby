version: "3.9"

services:
  tests:
    build: .
    command: bundle exec rspec -f json -o rspec.json
    volumes:
      # bind mount a directory to share the output of the test runner
      - type: bind
        source: .
        target: /code
